{
  "title": "Debug Exporter Configuration",
  "type": "object",
  "properties": {
    "verbosity": {
      "type": "string",
      "title": "Verbosity",
      "enum": [
        "basic",
        "normal",
        "detailed"
      ],
      "default": "basic",
      "description": "Level of detail for the output"
    },
    "sampling_initial": {
      "type": "integer",
      "title": "Initial Sampling Rate",
      "default": 2,
      "description": "Number of telemetry items to export before sampling begins"
    },
    "sampling_thereafter": {
      "type": "integer",
      "title": "Sampling Thereafter",
      "default": 1,
      "description": "Export every Nth telemetry item after initial sampling"
    },
    "use_internal_logger": {
      "type": "boolean",
      "title": "Use Internal Logger",
      "default": true,
      "description": "Write output using the collector’s internal logger"
    }
  }
}